{
  "name": "sparq",
  "version": "0.1.0",
  "description": "Sparq — gamified ADHD companion app with virtual pet, rival system, and neurodivergent community",
  "type": "module",
  "scripts": {
    "build:web": "node scripts/build-web.js",
    "android": "npm run build:web && npx cap sync android && npx cap open android",
    "build:android": "npm run build:web && npx cap sync android && npx cap build android",
    "add:android": "npx cap add android",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@capacitor-firebase/authentication": "^6.3.1",
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/local-notifications": "^6.0.0",
    "@capacitor/splash-screen": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
