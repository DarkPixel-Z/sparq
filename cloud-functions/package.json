{
  "name": "sparq-cloud-functions",
  "version": "1.0.0",
  "description": "Firebase Cloud Functions for Sparq — avatar moderation pipeline.",
  "main": "moderateAvatar.js",
  "engines": { "node": "20" },
  "scripts": {
    "deploy:moderation": "firebase deploy --only functions:moderateAvatar,functions:resolveAvatarReview",
    "deploy:rules": "firebase deploy --only storage:rules,firestore:rules"
  },
  "dependencies": {
    "@google-cloud/vision": "^4.0.2",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  }
}
